# std::clamp

## std::clamp(value, min, max)
**Parameters:**  
- value: any value
- min: any value
- max: any value

**Return value:**  
any value

**Descriptions:**
Returns min if value &lt; min and max if value &gt; max. Otherwise value is returned.