# std::is_good

### std::is_good(stream)
since 1.2  
**Parameters**
* stream: stream to check

**Return value**  
Bool

**Description**  
Returns true if no error occured and stream has not passed eof.