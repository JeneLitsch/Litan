function main() {
	var clock = ltn::clock();
	var foo = ltn::struct();
	foo.a = 0;
	for i (0, 1000000) {
		foo.b = 1;
		foo.b = ltn::null();
		foo.a += 1;
	}
	ltn::println(ltn::toSeconds(clock) + "s");
}