typedef str;

fnx delete(str string) voi {
	asm {
		"load 0"
		"heap::del"
	}
	return;
}

// create new string with merged content of str1 and str2
fnx add(str string1, str string2) str {
	asm {
		"load 0"
		"load 1"
		"string::add"
		"return"
	}
}