namespace ltn {
	asm clock() {
		"newclock"
		"return"
	}

	asm toSeconds(clock) {
		"cast_float"
		"return"
	}

	asm toMilliseconds(clock) {
		"cast_float"
		"newf 1000.0"
		"mlt"
		"return"
	}

	function wait(seconds) {
		var clock = clock();
		var start = toSeconds(clock);
		var current = start;
		while(current - start <= seconds) {
			current = toSeconds(clock);
		}
	}
}