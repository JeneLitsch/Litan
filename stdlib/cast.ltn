namespace std {
	asm int(x) {
		"cast_int"
		"return"
	}

	asm float(x) {
		"cast_float"
		"return"
	}

	asm string(x) {
		"cast_string"
		"return"
	}

	asm bool(x) {
		"cast_bool"
		"return"
	}

	asm array(x) {
		"cast_array"
		"return"
	}
}