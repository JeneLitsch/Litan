namespace std {
	asm queue() {
		"newqueue"
		"return"
	}

	asm stack() {
		"newstack"
		"return"
	}

	asm map() {
		"newmap"
		"return"
	}

	asm push(container, elem) {
		"push"
	}

	asm pop(container) {
		"pop"
		"return"
	}

	asm peek(container) {
		"peek"
		"return"
	}

	asm contains(map, key) {
		"contains"
		"return"
	}
}