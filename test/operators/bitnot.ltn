namespace op {
	function bitnot(sphinx) {
		var cases = [
			sphinx::assert_result([0], -1),
			sphinx::assert_result([1], -2),
			sphinx::assert_result([-1], 0),
		];
		
		var fx = lambda (x) => ~x;

		sphinx::run_test(sphinx, "Operator ~", fx, cases);
	}
}