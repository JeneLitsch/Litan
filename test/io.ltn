function main() {
	var fout = std::fout("test/files/output.txt");
	var fin = std::fin("test/files/input.txt");
	while(!std::is_eof(fin)) {
		var line = std::readln(fin);
		std::println(fout, line);
		std::println(fout, line);
		std::println(line);
	}
}