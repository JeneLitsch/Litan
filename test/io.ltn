function main() {
	var fout = ltn::fout("test/files/output.txt");
	var fin = ltn::fin("test/files/input.txt");
	while(!ltn::isEof(fin)) {
		var line = ltn::readln(fin);
		ltn::println(fout, line);
		ltn::println(fout, line);
		ltn::println(line);
	}
}