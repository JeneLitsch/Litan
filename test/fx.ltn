function f1() { return 42; }
function f2() { return 1337; }

function f(a, b) {
	return a - b;
}

function makeAdd() {
	return function(a, b) {
		return a + b;
	};
}

function main() {
	ltn::println("=== Lambdas ===");
	var add = makeAdd();
	ltn::println("add(1, 2) = " + ltn::invoke(add, [1, 2]));
	ltn::println("add(20, 22) = " + ltn::invoke(add, [20, 22]));

	ltn::println("=== Function pointers ===");
	var fxPtr = &f(_,_);
	ltn::println(ltn::invoke(fxPtr, [f1(), f2()]));
}