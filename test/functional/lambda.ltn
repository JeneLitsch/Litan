function make_lambda_1(a, b) {
	return lambda [a, b] () {
		std::println("1st Lambda call: " + a + " + " + b + " = " + (a+b)); 
		return a + b;
	};
}

function make_lambda_2() {
	return lambda () {
		std::println("2st Lambda call"); 
	};
}

function main() {
	var f1 = make_lambda_1(20, 22);
	std::println("Result: " + std::invoke(f1));

	var f2 = make_lambda_2();
	std::invoke(f2);
}