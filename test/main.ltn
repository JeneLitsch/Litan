function main() {
	var tests = [
		&test::arith(),
		&test::compare(),
		&test::queue(),
		&test::stack(),
		&test::array(),	
		&test::exception(),
		&test::math(),
		&test::bool(),
		&test::range(),
		&test::algorithm(),
		&test::struct(),
		&test::types(),
		&test::loops(),
		&test::namespacing(),
	];


	var errors = run_tests(tests);
	interprete_result(errors);
	return errors;
}
except(e) {
	std::println(e);
}