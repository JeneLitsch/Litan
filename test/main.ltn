function main() {
	var tests = [
		&test::add(),
		&test::sub(),
		&test::mlt(),
		&test::div(),
		&test::mod(),

		&test::eql(),
		&test::uneql(),
		&test::smaller(),
		&test::bigger(),
		&test::smaller_eql(),
		&test::bigger_eql(),

		&test::queue(),
		&test::array(),
		
		&test::exception(),
		
		&test::math(),
		
		&test::bool(),

		&test::range(),
	];


	var errors = run_tests(tests);
	interprete_result(errors);
	return errors;
}
except(e) {
	std::println(e);
}