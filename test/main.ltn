function main() {
	var tests = [
		&test::algorithm(),
		&test::arith(),
		&test::array(),	
		&test::bool(),
		&test::captures(),
		&test::compare(),
		&test::conditional(),
		&test::exception(),
		&test::loops(),
		&test::math(),
		&test::namespacing(),
		&test::queue(),
		&test::range(),
		&test::stack(),
		&test::struct(),
		&test::types(),
	];


	var errors = run_tests(tests);
	interprete_result(errors);
	return errors;
}
except(e) {
	std::println(e);
}