function main() {
	var fout = std::fout("tmp/files/output.txt");
	var fin = std::fin("tmp/files/input.txt");
	while(!std::is_eof(fin)) {
		var line = std::readln(fin);
		std::println(fout, line);
		std::println(fout, line);
		std::println(line);
	}
}