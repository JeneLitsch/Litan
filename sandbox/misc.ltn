function fib(n) => choose(n) {
	case 0: 0
	case 1: 1
	default: fib(n - 1) + fib(n - 2)
}

function main() {
	var clock = std::chrono::clock();
	var value = fib(35);
	std::println(value);
	std::println(std::float(clock));
}