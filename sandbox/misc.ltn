function main() {
	var libtest = std::lib::open("./bin/libtest.so");
	var hello_world = std::lib::get_fx(libtest, "hello_world");
	var print = std::lib::get_fx(libtest, "print");
	var str = hello_world() + "\n";
	print(str);

	std::println("AAA");
	var make = std::lib::get_fx(libtest, "make");
	var use = std::lib::get_fx(libtest, "use");
	var s = make();
	use(s);
}