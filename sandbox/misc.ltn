function powi_3(x) => x * x * x

function main() {
	var libtest = std::lib::open("bin/libtest.so");
	var hello_world = std::lib::get_fx(libtest, "hello_world");
	var print = std::lib::get_fx(libtest, "print");
	var str = hello_world();
	print(str + "\n");
}