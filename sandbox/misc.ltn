define pi = 3.14;
global x;

namespace foo::bar { 
	namespace baz { 
		function f(x) {
			global x = 42;
			return x;
		}
	}
}

function main() {
	std::println(foo::bar::baz::f(pi));
	std::println(global x);
}
