function main(args) {
	// var clock = std::chrono::clock();

	// // for(i : {0, 10000000}) {}
	// for(i : std::range(0, 10000000, 1)) {}
	// for(i : std::range(0, 10000000)) {}

	// std::println(std::chrono::to_seconds(clock));

	// var iter = std::range(1, 3);
	var iter = std::iter([1, 3]);
	std::println(std::next(iter));
	std::println(std::next(iter));
	std::println(std::next(iter));
	std::println(std::next(iter));
	std::println(std::next(iter));
	std::println(std::next(iter));
}