global foo;

namespace foo {
	global bar;
}

function f() {
	global foo = 3.1;
	global foo::bar::baz = 1.9;
}

function main(args) {
	f();
	global foo += global foo::bar;
	std::println(global foo);
	return global foo;
}
except(error) {
	std::println(error);
}