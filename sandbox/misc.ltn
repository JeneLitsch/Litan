function fib(n) {
	if(n == 0) return 0;
	if(n == 1) return 1;
	return fib(n - 1) + fib(n-2);
}

function main() {
	var clock = std::chrono::clock();
	var value = fib(35);
	std::println(value);
	std::println(std::float(clock));
}