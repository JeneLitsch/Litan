function map(arr) {
	var map = std::map();
	for(elem : arr) {
		map[elem[0]] = elem[1];
	}
	return map;
}


namespace foo {
	function f(a, b...) => b
}



function main(args) {
	var f1 = lambda(a, args...) => args;
	var f2 = lambda(a) => a;
	// std::println(std::is_variadic(f1));
	// std::println(std::is_variadic(f2));

	std::println(std::invoke(f1, (1,)));
	std::println(std::invoke(f2, (1,)));
	std::println(());
}