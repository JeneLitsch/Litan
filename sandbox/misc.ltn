function exchange(current, new) {
	var tmp = deref current;
	deref current = new;
	return tmp;
}


function main(args) {
	var a = 1;
	std::println(exchange(ref a, 2));
	std::println(exchange(ref a, 3));
	std::println(exchange(ref a, 4));
}