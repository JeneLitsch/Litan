function main(args) {
	var obj = [
		.foo = [
			.f = lambda (self, a, b : int, c) const {
				return self.x + a + b + c;
			},
			.x = 4,
		],
	];
	std::println(obj.foo->f(1,2.1 ,3));

	var f = lambda (a : string) -> int {
		return a;
	};

	return f("");
}