
fnx fib (int end) voi {
	int curr;
	int last;
	flt a;
	curr = 1;
	last = 0;
	for(i, 1, end) {
		->print(curr);
		int next;
		next = ->fibNext(last, curr);
		last = curr;
		curr = next;
	}
	return;
}

fnx fibNext(int last, int curr) int {
	return last + curr;
}
