struct Bar {
	var Int x;
}

struct Foo {
	var Bar bar;
	var Int test;
}

function fx(Int a, Int b) -> Foo {
	var Foo foo = new Foo(new Bar(a), b);
	return foo;
}

function main() -> Void {
	var Array arr = ->Array();
	->pushBack(arr, 1.1);
	->pushBack(arr, 2.1);
	->pushBack(arr, 3.1);

	->print(->at(arr, 0));
	->print(->at(arr, 1));
	->print(->at(arr, 2));

	->print(->Int(4.6));

	var Int a = 42;
	var String string1 = "Hello World";
	->print(string1);
	->print(a);
	return;
}