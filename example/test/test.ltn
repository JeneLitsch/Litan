fnx main() voi {
	repeat(5) {
		->print(42);
	}

	arr array;
	array = ->newArray();

	->addToArray(array, 10);
	->addToArray(array, 20);
	->addToArray(array, 30);
	->print(->readArrayI(array, 0));
	->print(->readArrayI(array, 1));
	->print(->readArrayI(array, 2));
	->delArray(array);
	return;
}
