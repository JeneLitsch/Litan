struct Bar {
	var Int x;
}


struct Foo {
	var Bar bar;
	var Int test;
}


function fx(Int a, Int b) -> Void {
	->print(->min(a, b));
	->print(->max(a, b));
}


function fx(Float a, Float b) -> Void {
	->print(->min(a, b));
	->print(->max(a, b));
}


function main() -> Void {
	var Array arr = ->Array();
	->pushBack(arr, 1.1);
	->pushBack(arr, 2.1);
	->pushBack(arr, 3.1);

	->print(->Float(->at(arr, 0)));
	->print(->Float(->at(arr, 1)));
	->print(->Float(->at(arr, 2)));

	->fx(-7, 42);
	->fx(-7.3, 42.2);

	var Int a = 42;
	->print("Hello World");
	->print(a);
}