// function fib(n) {
// 	if(n <= 0) return 0;
// 	if(n == 1) return 1;
// 	return ->fib(n - 1) + ->fib(n - 2);
// }

namespace foo {
    namespace bar {
        function fx() {
            return 41;
        }
    }
    function fx() {
        return bar::fx() + 1;
    }
}

function main() {
	var in = ltn::cin();
	var out = ltn::cout();
	ltn::print(out, "a = ");
	var a = ltn::readfloat(in);
	ltn::print(out,"b = ");
	var b = ltn::readfloat(in);
	ltn::println(out, "Result a + b = " + (a + b));
}