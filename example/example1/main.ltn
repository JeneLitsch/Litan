// function fib(n) {
// 	if(n <= 0) return 0;
// 	if(n == 1) return 1;
// 	return ->fib(n - 1) + ->fib(n - 2);
// }

namespace foo {
    namespace bar {
        function fx() {
            return 41;
        }
    }
    function fx() {
        return bar::fx() + 1;
    }
}

function main() {
    foo::bar::fx();
    return foo::fx();
}